<section class="residential-value">
    <div class="container">

        <div class="residential-value-layout">

            <div class="residential-value-content">
                <span class="eyebrow">LONG-TERM VALUE</span>

                <h2>
                    Solar should save you money today and for years to come.
                </h2>

                <p>
                    The cheapest solar system is not always the best investment.
                    Solar8 focuses on designing a system that balances energy
                    production, battery storage, backup requirements and future
                    electricity usage.
                </p>

                <p>
                    A properly designed system can help reduce your dependence
                    on the grid while giving your household greater control
                    over its energy costs.
                </p>

                <a href="/assessment/" class="value-button">
                    Assess My Home
                    <span aria-hidden="true">→</span>
                </a>
            </div>

            <div class="residential-value-card">

                <div class="value-card-header">
                    <div class="value-card-icon" aria-hidden="true">
                        <svg
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <path d="M12 3v18" />
                            <path d="M17 7H9.5a3.5 3.5 0 0 0 0 7H15a3.5 3.5 0 0 1 0 7H7" />
                        </svg>
                    </div>

                    <div>
                        <span>YOUR INVESTMENT</span>
                        <h3>Think beyond the equipment.</h3>
                    </div>
                </div>

                <div class="value-list">

                    <div class="value-item">
                        <div class="value-number">01</div>
                        <div>
                            <h4>Reduce grid dependence</h4>
                            <p>
                                Generate more of your own electricity instead
                                of buying all of it from the grid.
                            </p>
                        </div>
                    </div>

                    <div class="value-item">
                        <div class="value-number">02</div>
                        <div>
                            <h4>Protect against rising tariffs</h4>
                            <p>
                                Solar generation can reduce the amount of
                                electricity your household needs to purchase.
                            </p>
                        </div>
                    </div>

                    <div class="value-item">
                        <div class="value-number">03</div>
                        <div>
                            <h4>Build energy resilience</h4>
                            <p>
                                Battery storage provides backup power when
                                the electricity supply is unavailable.
                            </p>
                        </div>
                    </div>

                    <div class="value-item">
                        <div class="value-number">04</div>
                        <div>
                            <h4>Plan for the future</h4>
                            <p>
                                A well-designed system can accommodate changing
                                household needs and future expansion.
                            </p>
                        </div>
                    </div>

                </div>

            </div>

        </div>

    </div>
</section>

<style>
.residential-value {
    width: 100%;
    padding: 120px 0;
    background: var(--color-white);
}

.residential-value-layout {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    gap: 80px;
    align-items: center;
}

.residential-value-content {
    max-width: 650px;
}

.residential-value-content h2 {
    margin: 16px 0 24px;
    color: var(--color-heading);
    font-family: var(--font-heading);
    font-size: clamp(2.35rem, 4vw, 3.3rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.035em;
}

.residential-value-content p {
    margin-top: 18px;
    color: var(--color-muted);
    font-size: 17px;
    line-height: 1.8;
}

.value-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    min-height: 56px;
    margin-top: 32px;
    padding: 0 26px;
    border-radius: var(--button-radius);
    background: var(--color-primary);
    color: var(--color-white);
    font-size: 15px;
    font-weight: 800;
    text-decoration: none;
    box-shadow: 0 10px 24px rgba(255, 104, 0, 0.18);
    transition:
        background 0.25s ease,
        transform 0.25s ease,
        box-shadow 0.25s ease;
}

.value-button span {
    font-size: 20px;
    line-height: 1;
    transition: transform 0.25s ease;
}

.value-button:hover {
    background: var(--color-primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 14px 30px rgba(255, 104, 0, 0.22);
}

.value-button:hover span {
    transform: translateX(4px);
}

.residential-value-card {
    padding: 42px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-large);
    background: #f8f9fa;
    box-shadow: var(--shadow-light);
}

.value-card-header {
    display: flex;
    align-items: center;
    gap: 20px;
    padding-bottom: 30px;
    border-bottom: 1px solid var(--color-border);
}

.value-card-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    flex: 0 0 auto;
    width: 64px;
    height: 64px;
    border-radius: 18px;
    background: rgba(255, 104, 0, 0.1);
    color: var(--color-primary);
}

.value-card-icon svg {
    width: 30px;
    height: 30px;
}

.value-card-header span {
    display: block;
    color: var(--color-primary);
    font-size: 12px;
    font-weight: 800;
    letter-spacing: 0.12em;
}

.value-card-header h3 {
    margin-top: 6px;
    color: var(--color-heading);
    font-family: var(--font-heading);
    font-size: 24px;
    font-weight: 800;
    line-height: 1.25;
}

.value-list {
    display: flex;
    flex-direction: column;
}

.value-item {
    display: grid;
    grid-template-columns: 48px minmax(0, 1fr);
    gap: 20px;
    padding: 26px 0;
    border-bottom: 1px solid var(--color-border);
}

.value-item:last-child {
    padding-bottom: 0;
    border-bottom: 0;
}

.value-number {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 42px;
    height: 42px;
    border: 1px solid rgba(255, 104, 0, 0.2);
    border-radius: 12px;
    background: var(--color-white);
    color: var(--color-primary);
    font-size: 12px;
    font-weight: 800;
}

.value-item h4 {
    margin: 0;
    color: var(--color-heading);
    font-family: var(--font-heading);
    font-size: 18px;
    font-weight: 800;
    line-height: 1.3;
}

.value-item p {
    margin: 7px 0 0;
    color: var(--color-muted);
    font-size: 14px;
    line-height: 1.65;
}

@media (max-width: 1100px) {
    .residential-value-layout {
        grid-template-columns: 1fr;
        gap: 56px;
    }

    .residential-value-content {
        max-width: 800px;
    }

    .residential-value-card {
        max-width: 900px;
    }
}

@media (max-width: 900px) {
    .residential-value {
        padding: 88px 0;
    }

    .residential-value-layout {
        gap: 44px;
    }
}

@media (max-width: 640px) {
    .residential-value {
        padding: 72px 0;
    }

    .residential-value-content h2 {
        font-size: 2.2rem;
    }

    .residential-value-content p {
        font-size: 16px;
    }

    .residential-value-card {
        padding: 28px 24px;
        border-radius: 20px;
    }

    .value-card-header {
        align-items: flex-start;
        gap: 16px;
        padding-bottom: 24px;
    }

    .value-card-icon {
        width: 56px;
        height: 56px;
        border-radius: 16px;
    }

    .value-card-icon svg {
        width: 27px;
        height: 27px;
    }

    .value-card-header h3 {
        font-size: 21px;
    }

    .value-item {
        grid-template-columns: 42px minmax(0, 1fr);
        gap: 14px;
        padding: 22px 0;
    }

    .value-number {
        width: 38px;
        height: 38px;
        border-radius: 10px;
    }

    .value-item h4 {
        font-size: 17px;
    }
}
</style>