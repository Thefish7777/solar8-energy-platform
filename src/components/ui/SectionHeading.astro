---
interface Props {
    tag: string;
    title: string;
    intro?: string;
}

const { tag, title, intro } = Astro.props;
---

<div class="section-heading centered">
    <span class="eyebrow">{tag}</span>
    <h2>{title}</h2>
    {intro && <p>{intro}</p>}
</div>
